<!DOCTYPE html>
<html>
<head>
  <title>Login</title>
  <style>
    body {
  display: flex;
  justify-content: center;
  align-items: center;
  height: 100vh;
  margin: 0;
  background-color: #f2f2f2;
  font-family: Arial, sans-serif;
}

.container {
  width: 400px; /* Set a fixed width for the container */
  padding: 30px; /* Increase the padding for the container */
  background-color: #fff;
  border-radius: 5px;
  box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
}

h1 {
  text-align: center;
  font-size: 24px; /* Increase the font size for h1 */
  margin-bottom: 20px;
}

.form-group {
  margin-bottom: 20px; /* Increase the margin bottom for form groups */
}

label {
  font-weight: bold;
  display: block;
  margin-bottom: 10px; /* Increase the margin bottom for labels */
}

input[type="email"],
input[type="password"] {
  width: 100%;
  padding: 10px; /* Decrease the padding for input fields */
  font-size: 16px; /* Decrease the font size for input fields */
  border: 1px solid #ccc;
  border-radius: 3px;
}

button[type="submit"] {
  display: block;
  width: 100%;
  padding: 12px; /* Decrease the padding for submit button */
  background-color: #4CAF50;
  color: #fff;
  border: none;
  border-radius: 3px;
  cursor: pointer;
  font-size: 16px; /* Decrease the font size for submit button */
}

a {
  color: #4CAF50;
  text-decoration: none;
  font-size: 16px; /* Decrease the font size for links */
}

  </style>
</head>
<body>
  <div class="container">
    <h1>Login</h1>
    <form action="/login" method="POST">
      <div class="form-group">
        <label for="email">Email</label>
        <input type="email" id="email" name="username" required>
      </div>
      <div class="form-group">
        <label for="password">Password</label>
        <input type="password" id="password" name="password" required>
      </div>
      <div class="form-group">
        <button type="submit">Login</button>
      </div>
    </form>
    <div class="form-group">
      <a href="/sign-up">Sign Up</a>
    </div>
  </div>
</body>
</html>
